# Dashboard + API behind one origin: static SPA with /api proxied to the api
# service. Same-origin means cookies and CORS behave with zero configuration.
server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  gzip on;
  gzip_types text/css application/javascript application/json image/svg+xml;

  location /api/ {
    proxy_pass http://api:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
  location = /health { proxy_pass http://api:8080; }
  location = /ready  { proxy_pass http://api:8080; }

  # SPA fallback
  location / {
    try_files $uri /index.html;
  }
}
